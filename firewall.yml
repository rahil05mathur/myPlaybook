---
- name: firewall changes
  hosts:

  tasks:
  - name: Enable a service in firewalld
    firewalld:
      service: nginx
      permanent: true
      state: enabled
  - name: reload the firewalld
    service:
      name: firewalld
      state: reloaded
